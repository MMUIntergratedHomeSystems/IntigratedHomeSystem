<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mmu.tom.linkedviewproject" >
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:allowBackup="true"  android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name="mmu.tom.linkedviewproject.MainActivity" android:parentActivityName="mmu.tom.linkedviewproject.HomeActivity" >

        </activity>
        <activity android:name="mmu.tom.linkedviewproject.DeviceDetailsActivity" android:label="@string/app_name" android:parentActivityName="mmu.tom.linkedviewproject.MainActivity" >

        </activity>
        <activity android:name="mmu.tom.linkedviewproject.HomeActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
